import "./InputChartSection.css"

function InputChartSection({className}) {
  return (
    <div className={`input-chart-section ${className ? className : ''}`}></div>
  )
}

export default InputChartSection;